import { CommentFormatPipe } from './comment-format.pipe';

describe('CommentFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
