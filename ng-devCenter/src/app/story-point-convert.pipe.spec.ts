import { StoryPointConvertPipe } from './story-point-convert.pipe';

describe('StoryPointConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryPointConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
