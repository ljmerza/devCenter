import { DropdownSubmenuDirective } from './dropdown-submenu.directive';

describe('DropdownSubmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownSubmenuDirective();
    expect(directive).toBeTruthy();
  });
});
