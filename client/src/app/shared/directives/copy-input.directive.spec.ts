import { CopyInputDirective } from './copy-input.directive';

describe('CopyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyInputDirective();
    expect(directive).toBeTruthy();
  });
});
